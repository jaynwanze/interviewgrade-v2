packages:
  - '.'

# pnpm 11 blocks dependency build scripts by default. These packages are
# required by the framework/tooling and are explicitly allow-listed rather
# than enabling arbitrary install scripts.
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
