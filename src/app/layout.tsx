import type { Metadata } from 'next';
import type { ReactNode } from 'react';

import './globals.css';

export const metadata: Metadata = {
  title: {
    default: 'InterviewGrade',
    template: '%s | InterviewGrade',
  },
  description:
    'Create realistic AI-powered practice exercises with rubric-based feedback.',
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
